## Goal
Complete Steps 2–7 in `L:\MausamSetu\MSatu`, preserving the already-created Step 1 files and working only inside the current repository. The resulting project will be a structural scaffold: implementation exists only where the specification explicitly requires live infrastructure (`/health`, WebSocket heartbeats, frontend health query, MapLibre/Cesium boot, and 9 display-only zone markers). All domain/data/model/training operations remain typed `NotImplementedError` stubs.

## Current state confirmed
- Git repository is initialized on `main`; it has no commits and all Step 1 files are untracked.
- Required directories and `.gitkeep` files exist, along with `.gitignore`, `README.md`, and `.env.example`.
- Environment audit passed: Python 3.12.10, Node 24.18.0, pnpm 11.17.0, NVIDIA RTX 3060 Laptop GPU (6 GB), driver 555.97 / CUDA 12.5; `uv` is the only missing prerequisite.

## Implementation plan

1. **Establish Python workspace and lock dependencies**
   - Install `uv` using its official Windows installer and verify its version.
   - Create root `pyproject.toml` as a uv workspace with `packages/core` and `apps/api`; configure Python `>=3.12`, Ruff, strict mypy, pytest paths/markers, and a dedicated explicit PyTorch CUDA index.
   - Create package metadata for `mausamsetu-core` and `mausamsetu-api`, using editable workspace resolution for the API’s core dependency. Declare all requested direct dependencies and API dev dependencies.
   - Resolve PyTorch strictly from the PyTorch CUDA 12.4 wheel index (`cu124`), which is compatible with the detected 12.5 driver. No CPU wheel source or fallback will be configured.
   - Run `uv sync`; if the resolver/install fails or `torch.cuda.is_available()` is false, stop immediately and report rather than substituting a package/build.
   - Record fully resolved versions in `uv.lock` and report every direct dependency’s selected version.

2. **Create the importable core skeleton**
   - Replace the core placeholder with a `src/mausamsetu_core` package and `py.typed`.
   - Add `config`, `data`, `regions`, `model`, `train`, `eval`, and `runtime` subpackages with focused modules matching the requested responsibilities (settings/experiment schemas; readers/cube/manifest; 9-zone registry; ConvLSTM/FiLM; trainer/loop/callbacks; metrics/baselines/validation; executor/event-queue/device).
   - Use typed public interfaces, small Pydantic schemas where structure is required, docstrings, and `raise NotImplementedError` for all unavailable domain behavior. The nine-zone registry will expose names/centroids only for UI scaffolding—not data, forecasting, or training logic.
   - Add import coverage in `packages/core/tests/test_imports.py` and configure strict mypy to validate the core and API source packages.

3. **Build the FastAPI skeleton**
   - Create the `src/mausamsetu_api` package with an app factory, dependency providers, resource schemas, structured JSON logging, and versioned routers.
   - Implement real `GET /health` only: gather Python/Torch/CUDA/GPU memory information from the device detected once in lifespan, return the exact requested response fields, generate ISO-8601 IST timestamps via `ZoneInfo("Asia/Kolkata")`, and leave model/data-manifest placeholders as `false`/`null`.
   - Configure CORS for `http://localhost:5173`; log detected device state with structlog during lifespan and retain it on `app.state`.
   - Add typed, explicit `NotImplementedError` stubs to all REST endpoint handlers except health. Implement the requested run-events WebSocket as plumbing only: accept then send a heartbeat payload every two seconds until disconnect.
   - Add API tests for health response and WebSocket heartbeat, then smoke-test Uvicorn plus `/docs`.

4. **Scaffold and wire the SvelteKit frontend**
   - Generate a SvelteKit TypeScript application in `apps/web` with ESLint, Prettier, Vitest, and Playwright, then establish a pnpm workspace/lockfile from the repo root.
   - Configure Tailwind CSS v4 through the Vite plugin and CSS-first `@theme` tokens (no `tailwind.config.js`).
   - Install the requested visualization/data packages at the resolved locked versions. Add `@sveltejs/adapter-static` as the minimal required build adapter because the requested web Docker image must produce static output; configure the SvelteKit fallback appropriately.
   - Configure Cesium static copying with `vite-plugin-static-copy`, define `window.CESIUM_BASE_URL = "/cesium"`, and test that copied Workers/Assets/Widgets/ThirdParty are served.
   - Initialize shadcn-svelte and generate exactly the named primitives: button, card, tabs, select, slider, switch, table, badge, separator, skeleton, tooltip, and dialog.
   - Add shared layout/sidebar, region selector, device/health badge, and route placeholders for `/`, `/map`, `/globe`, `/forecast`, `/training`, `/validation`, `/registry`, and `/zones`.
   - Create typed frontend modules for API fetches, Zod response validation, TanStack Query health query, URL-synced regional state, MapLibre/deck wrappers, Cesium wrapper, and lazily loaded Plotly wrapper.
   - Implement only the required visible bootstrap behavior: live sidebar health data, India-centered Cesium globe/terrain, India-centered MapLibre map with an empty deck overlay plus the 9 static zone-centroid display markers, and a test-facing run-events heartbeat status. No forecast, model, training, validation, persistence, or data-processing logic will be added.
   - Create local `.env` only if needed to run the frontend; it remains ignored. Keep the committed `.env.example` current with all Python and public web variables.

5. **Add tooling, containers, and CI**
   - Create `.pre-commit-config.yaml` with Ruff lint/format, strict mypy, end-of-file fixer, trailing whitespace, 20 MB added-file cap, and private-key detection; install the local hook.
   - Create a Windows-compatible Makefile with requested setup, API, web, concurrent dev, test, lint, format, and cache-clean commands. Commands will use uv/pnpm and invoke a small cross-platform concurrent process strategy rather than assuming Unix-only `&` behavior.
   - Add `docker/Dockerfile.api` based on Python 3.12 slim with uv, and document the alternate CUDA base image/GPU-run guidance rather than assuming passthrough. Add static-output `docker/Dockerfile.web` based on Node 20.
   - Add `docker-compose.yml` with API/web services and mounts for ignored `data/` and `checkpoints/`, no automatic GPU reservation.
   - Add GitHub Actions CI for lint, strict type check, and test. Define/register a `gpu` pytest marker so GPU-only checks skip in hosted CI.

6. **Verify layer by layer, then commit locally**
   - Verify CUDA Torch with the exact requested Python command and retain output.
   - Run core import tests and strict mypy; API health and WebSocket tests; frontend type/lint/unit checks; production frontend build; targeted Playwright smoke for map/globe where browser tooling is available.
   - Run `make lint`, `make test`, and `docker compose build`; fix any errors before reporting completion. If Docker Desktop is unavailable, report that limitation with the exact command output instead of claiming success.
   - Run `make dev`, inspect `/health`, Swagger, WebSocket heartbeat, and frontend routes. Capture screenshots of `/map`, `/globe`, and the final end-to-end UI state.
   - Check staged file sizes and ignored artifacts, stage only intended source/config/lock files, and create the local commit exactly: `chore: scaffold monorepo`. Do not push or alter the default branch.

## Expected files created
- Root: `pyproject.toml`, `uv.lock`, `pnpm-workspace.yaml`, root package metadata as needed, `Makefile`, pre-commit config, Compose file, updated docs/env example.
- Python: both package `pyproject.toml` files, `src` packages, tests, API routes/schemas/logging.
- Web: standard SvelteKit sources/config/tests, Tailwind/Vite/Cesium integration, component and API scaffold directories, `pnpm-lock.yaml`.
- Delivery: Dockerfiles, CI workflow, and screenshots/test artifacts only if they are appropriately ignored or small and explicitly desired.

## Stop conditions
I will stop and report immediately—without changing to a CPU build or alternate library—if CUDA PyTorch cannot be installed/recognized, a requested dependency cannot install, or the target build tooling is unavailable. I will also preserve the no-data/no-checkpoint/no-secret/no-file-over-20MB constraints throughout.
