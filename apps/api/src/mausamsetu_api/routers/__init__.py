"""Versioned routers exposed by the API."""

from mausamsetu_api.routers import forecast, health, models, runs, tiles, validate, zones

__all__ = ["forecast", "health", "models", "runs", "tiles", "validate", "zones"]
